# Failure Matrix — Tiered Model Routing / Escalation Signal Reliability

| # | Scenario | Prediction (verbatim) | Observed | Verdict | Takeaway |
|---|---|---|---|---|---|
| S2 | Code-path coverage: run the intent matcher alone over all 30 queries. | *(skipped — my harness-design error: the "steady-state check" was a literal alias for S2's own logic, and I then restated the actual results in text. No real prediction was possible.)* | claim rate 10/30 (33.3%); false-claim rate among claimed 2/10 (20%). Trivial tier: 8/8 claimed, all correct. False claims: H01 (grabbed the wrong order_id — 10234 instead of 10567 — via first-digit-match regex, and answered with the wrong cancellation status) and H06 (regex matched "status" as a track keyword, grabbed order 10999, returned a bare "not found" that ignored the rest of the multi-issue dispute). | N/A (no prediction) | Naive regex intent matchers over-trigger on non-trivial text that happens to contain a 5-digit number plus an incidental keyword ("status", "cancel") embedded in unrelated context — coverage isn't free; it needs to be scoped to actual trivial-tier vocabulary, not just presence of a keyword. |

| S1 | Baseline: all 30 queries -> Sonnet only. No prediction required — this is the reference point S3 measures its quality delta against. | 28/30 correct (93.3%); cost $0.29412; tokens 34,810 in / 12,646 out; avg latency 5,241ms. 2 misses, both hard-tier: H04 (conflated two orders' distinct eligibility outcomes — 10321 past window vs 10788 within window — under one rule instead of two); H06 (correctly noted the order doesn't exist, then proceeded to reason at length about tracking/refund status for it anyway, contrary to the rubric's "just surface the missing ID" requirement). | N/A (no prediction) | Even the frontier baseline isn't perfect on the hardest tier — both misses are "recognized the right fact, then didn't let it terminate the reasoning" (H06) or "collapsed two similar-but-distinct cases into one" (H04). Sets 93.3% / $0.294 / ~5.2s as what S3's cascade needs to beat or approach at lower cost. My own cost estimate (README) was ~2x too low — didn't account for the full policy doc + order JSON embedded in every system prompt.|

| S3 | Cascade with self-reported confidence (threshold=0.7), Simple tier. | "3 queries escalate, resulting accuracy is 8/8." | Escalation: 1/8 (12.5%) — only S04. Accuracy: 8/8 (100%), matching prediction. Cost $0.00229, avg latency 1,982ms. | Partial (accuracy ✓, escalation count ✗ — predicted 3, observed 1) | Confidence didn't track difficulty in any legible way: the shortest, clearest message (S04) scored lowest confidence (0.5, escalated) while genuinely rambling ones (S01/S03/S05) scored 0.8. Self-reported confidence isn't a measured statistic — it's generated by the same next-token process as the rest of the output, so it defaults high whenever the surface form looks clean, independent of whether the underlying extraction is correct. Accuracy being 8/8 here means confidence *happened* to not mislead anyone this round — doesn't mean the signal is reliable in general (that's S4).|

| S4 | CORE: local model alone, Simple tier, no escalation. Of the wrong answers, how many carry confidence ≥0.7? | "5/8 wrong overall, 3 confidently wrong." | 1/8 wrong (S04 — order_id came back `undefined`), confidence on that answer was 0.5 (below threshold). 0 confidently-wrong cases. Cost $0, avg latency 2,154ms. | ✗ (both numbers) | Undersized sample for this specific metric: with only 1 wrong answer, "confidently wrong" can only read 0% or 100% for this run — not enough data to say anything about calibration in general. Real finding: `llama3.2:3b` is more capable at this extraction task than the lab design assumed, so it rarely errs, starving the exact failure mode S4 needed to observe. Doesn't refute the router-design concern — means 8 queries is underpowered to see it. Parking lot: a larger or deliberately-adversarial query set would be needed to test this claim properly; out of scope for this contract.|

| S5 | Verifier-based escalation (structural + Haiku plausibility check), Simple tier. | "1 escalates, resulting accuracy 8/8." | Escalation: 3/8 (37.5%) — S04 (structOk=false, the genuine miss), S01 and S08 (structOk=true, haikuOk=false — false-positive rejections of valid addresses). Accuracy: 8/8 (100%), same as S3. Cost $0.00852 — ~4x S3's $0.00229. | Partial (accuracy ✓, escalation count ✗ — predicted 1, observed 3) | Same accuracy as S3 for 4x the cost — the extra escalations bought nothing this round. The Haiku plausibility check isn't a ground-truth verifier, it's another LLM's soft judgment call — same fallible category as self-reported confidence, just repackaged and paid for. Only the structural half (regex/schema check) behaves like true ground truth, and it's the part that caught the one real defect (S04) — the same one confidence already caught for free. A "verifier" only buys real reliability to the extent its checks are actually deterministic; bolting on a second LLM call doesn't automatically improve calibration.|

| S6 | Control: local model vs Opus head-to-head, Simple tier. | "Opus 8/8, gap exists." | Local 7/8 (87.5%), Opus 8/8 (100%) — gap exists, exactly 1 query (S04, same miss as S3/S4). Opus cost $0.02491 vs local $0. | ✓ | Confirms the control has teeth — a real quality gap exists, but it's narrow (1/8) and costs ~$0.025 to close via Opus for this task. S04 has now failed identically across S3, S4, and S6 — a reproducible weakness on a specific short, direct-phrasing query, not random noise, which is itself informative: local-model failure here is systematic and could in principle be identified/fixed, not just a diffuse quality gap.|

| S7 | Threshold boundary sweep: 0.5 / 0.7 / 0.9, confidence cascade. | "Escalation 7/8 at 0.9, 1/8 at 0.7, 1/8 at 0.5. Knee at 0.7." | 0.5: escalation 0/8, accuracy 87.5%, cost $0. 0.7: escalation 1/8, accuracy 100%, cost $0.00172. 0.9: escalation 7/8, accuracy 100%, cost $0.02028 (12x the 0.7 cost for zero extra accuracy). Knee at 0.7 confirmed. | Partial (0.7 ✓, 0.9 ✓, knee location ✓; 0.5 ✗ — predicted 1/8, actual 0/8) | Classic boundary/off-by-one: the escalation check is strict `confidence < threshold`, so a wrong answer whose confidence exactly equals the threshold (S04 at exactly 0.5) does NOT escalate at threshold=0.5 — it slips through silently. This is the sharpest illustration of the lab's core claim: at the exact boundary, the confidently-wrong failure mode isn't hypothetical, it's mechanically guaranteed by `<` vs `<=`. The cost/quality frontier itself is clean and well-behaved (knee at 0.7, flat accuracy + linear cost growth beyond it) — the danger is entirely in the boundary condition, not the general shape.|


## 1B follow-up lab (new contract, same harness)

| # | Scenario | Prediction (verbatim) | Observed | Verdict | Takeaway |
|---|---|---|---|---|---|
| S8 | Control: 1B vs 3B local model, Simple tier. | "1b accuracy 5/8, 3b 7/8." | 1B: 3/8 (37.5%). 3B: 7/8 (87.5%, same S04 miss as every prior run). | Partial (3B ✓ exact; 1B ✗ — predicted 5/8, actual 3/8) | 1B's failure mode is systematic, not scattered: all 5 wrong answers failed identically on `order_id: undefined` — it isn't garbling addresses, it's dropping the order-number extraction specifically. And unlike 3B's one low-confidence miss, most of 1B's wrong answers carried the SAME confidence (0.8) as its correct ones — visible directly in the raw data, setting up S9. |
| S9 | CORE: confidence calibration, 1B model alone, no escalation. | *(skipped — same design flaw as S2: S9 is deterministically identical to S8's "1B" column, already fully visible before any prediction was possible.)* | Wrong: 5/8 (S01, S04, S05, S06, S07). Confidently wrong (conf≥0.7 AND wrong): 4/5 — S04/S05/S06/S07 all at conf=0.8, indistinguishable from its correct-answer confidence. Only S01 (conf=0.5) was wrong-and-appropriately-unconfident. | N/A (no prediction) | **The core claim, finally observed with real statistical weight (not n=1):** at 1B scale, this model is confidently wrong 80% of the time it's wrong. Self-reported confidence carries no discriminating signal between right and wrong answers here — 0.8 means nothing. This is what S4 was built to find and couldn't, purely because the 3B model didn't fail often enough to show it. |

| S10 | Confidence cascade (threshold=0.7) with 1B model — the sharpest test of the router design. | "1 escalates, 4/8 correct." | Escalation: 1/8 (12.5%, only S01). Accuracy: 4/8 (50%). Cost $0.00313. | ✓ Exact | The payoff scenario: identical router logic and threshold that produced 8/8 with the 3B model (S3) produces 50% with the 1B model — because 4 of 5 wrong answers sat at the same confidence (0.8) as correct ones and never triggered escalation. Same code, same threshold, opposite outcome, entirely determined by whether the underlying model's confidence happens to discriminate right from wrong. This is the concrete, measured version of "a confidence-threshold router silently ships wrong answers" — no longer hypothetical.|

## End-to-end router lab (new contract, same harness, S11)

| # | Scenario | Prediction (verbatim) | Observed | Verdict | Takeaway |
|---|---|---|---|---|---|
| S11-dry | Free classification-only check: mechanical router (code path → order-ID-count/dispute-keyword heuristic → local/Sonnet/Opus) over all 30 queries, no oracle tier label. | *(no prediction — steady-state check, run before any paid scenario, per the lesson from the earlier S2/S9 aliasing mistakes)* | 24/30 (80%) predicted routes matched `expected_route`. 6 misses: S05 (zip code `10021` false-triggers the "≥2 order IDs" complex signal alongside real order `10321`), H01+H06 (code path over-claims non-trivial queries containing a stray order-ID+keyword — same bug as S2), H02/H03/H05 (no mechanical tell for "contains a contradiction," under-escalated to Sonnet). | N/A (no prediction) | A generic, non-reverse-engineered structural heuristic gets 80% of real traffic to the right tier for free, but has two distinct blind spots: token-shaped-like-an-order-ID false positives (zip codes) and semantic-contradiction detection that no keyword count can see. |
| S11 | CORE: full end-to-end router, all 30 queries mixed, no tier hint at dispatch — does misrouting actually cost accuracy? | "30/30 overall accuracy; S05 and H06 land correct." | 28/30 (93.3%), cost $0.21628. Of the 6 route mismatches, only 2 produced wrong answers: S05 (Opus called under a prose-only `HARD_SYSTEM_PROMPT` that never asks for structured fields — grading failed on missing JSON, not on reasoning quality) and H01 (code path's canned single-order response, same bug as S2, can't address a 3-part dispute). The other 4 misroutes (H02, H03, H05, H06) all graded correct despite landing on the "wrong" tier. | ✗ (predicted 30/30; actual 28/30 — 2 misses, and the 2 that missed weren't the pair predicted) | **Misrouting a query to the wrong tier and getting the wrong answer are not the same event.** Sending a query to a more-or-less-capable reasoning model (Sonnet instead of Opus) cost nothing here 3 times out of 4 — reasoning quality degraded gracefully. What actually broke correctness was tier mismatches that changed the *response contract*: a rigid code-path template with no room for nuance, and an LLM prompt that structurally doesn't emit the field schema the grader expects. The real routing risk isn't "which model answers" so much as "does the chosen tier's output shape match what the caller needs" — a distinct failure class from anything the original escalation-signal lab (S1–S10) tested. |
| S12-dry | Fix S05's specific bug: `classifyQueryV2` excludes an order-ID-shaped token when it's immediately preceded by a bare 2-letter state abbreviation (the actual zip-code tell — "NY 10021" — rather than trying to enumerate real zip codes). Free classification check only, same 30 queries. | *(no prediction requested — narrow, targeted fix; verifying it doesn't need a guess)* | Route-match improved 24/30 → 25/30. S05 now correctly predicts `local_model`. H01/H06 (code-path false-claims) and H02/H03/H05 (contradiction-detection blind spot) unchanged — out of scope for this fix, left as known parking-lot items. H04 still correctly `complex` (both real order IDs still counted; the exclusion rule didn't over-fire). | N/A (no prediction) | The fix is surgical: it changes exactly one classification (S05) and nothing else, confirming the exclusion rule targets the actual mechanism (zip codes sit right after a state abbreviation) rather than pattern-matching the specific query. |
| S12 | CORE (paid run) — **not executed.** | — | — | — | Skipped by explicit user decision: S11 already showed S05's only failure mode was landing on Opus under a prompt with no JSON schema; fixing the classification routes it back through the same local-model cascade already validated in S3/S6/S8 for this exact query set, so the paid outcome is inferable from existing data rather than genuinely uncertain — spending to re-confirm it would repeat the S2/S9 steady-state-aliasing mistake in reverse. Left as a one-line follow-up (`node --env-file=../.env run.mjs s12`) if ever worth confirming for real. |

---
