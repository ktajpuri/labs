# Failure Matrix — dist-txn-lab

| # | Scenario | Prediction (verbatim) | Observed | Verdict | Takeaway |
|---|---|---|---|---|---|
| 1 | Naive dual-write, crash between commits; then operator rerun | "a) Alice - 900, bob - 1000, total - 1900 b) Invariant violated c) Cant fix, if transfer-naive.js is rerun it dedcts 100 from alice again, ALice - 800, bob - 1100, total - 1900" | 900/1000/1900 VIOLATED; rerun → 800/1100/1900 still VIOLATED | ✓ | A crash between independent commits loses money durably; naive retry double-debits because it can't know which half already happened. |
| 2 | 2PC against Postgres default `max_prepared_transactions=0` | "a) It fails at prpare transaction step b) alice: 1000, bob: 1000, total : 2000 nothing left in doubt c) Because it doesnt have built in coordinator system for 2PC" | Error `prepared transactions are disabled` at first PREPARE; state clean 1000/1000/2000 | ✓ (c half) | 2PC is opt-in: PREPARE is useless without an external coordinator — and (S3 will show) orphaned prepared txns are dangerous, so the default is off. |
| 3 | Coordinator dies after both prepares; contender; recovery | "a) alice: 1000, bob: 1000, total: 2000, invariant:ok. 2 in doubt transactions 1 in each bank b) the update blocks as the row is not released and still locked. It ends in timeout c) the transactions are rolled back as coordinator hasnt logged commit descisions d) unbounded" | 1000/1000/2000 OK + 1 in-doubt each bank (age 23s, growing); contender blocked 5009ms → lock timeout; recover → presumed ABORT both; locks unbounded | ✓ | In-doubt = voted YES, can't decide alone; locks held unbounded until a coordinator decision. Readers don't block (MVCC) — it's a writer problem. This is why max_prepared_transactions defaults to 0. |
| 4 | CONTROL: coordinator dies after logging COMMIT decision, before telling anyone | "a) alice: 1000, bob: 1000, totoal: 2000, 1 in doubt transactions per bank b) It commits this time, alice: 900, bob: 1100, total: 2000 c) The transaction is complete because coordinator has written the descision in log" | Identical in-doubt state to S3; recover → rolled FORWARD both → 900/1100/2000 | ✓ | S3 and S4 are indistinguishable to participants; one log line on the coordinator's disk flips abort→commit. The commit point of 2PC is the coordinator's log write, not any participant commit. |
| 5 | Participant (bank-b) restarted while holding a prepared txn | "a) its prepared transaction still in pg_prepared_xacts, its not cleared b) ye locks are still held on c) not sure of the name, but the transaction is across DB, one DB cant take a unilateral descision of commit or rollback, so it must be saved d) rollback and commit is not in log, balances 1000/1000/2000" | Prepared txn survived restart, age 23s continuous from original prepare; locks held; recover → presumed ABORT → 1000/1000/2000 | ✓ (c reasoned, mechanism unnamed) | A YES vote is a WAL write (pg_twophase): PREPARE persists the txn's full state before answering, so the promise survives crashes — unlike an ordinary open txn, which a restart aborts. |
| 6 | Saga, T2 fails → compensation C1 | "(a) Balances 1000/1000/2000, invariant ok (b) 2 transactions commited on bank a, in 2PC abort path 0 transactions commited (c) False, after debit from bank A and before fail-step 2 executed, readers would have seen total as 1900" | T1 committed, T2 failed, C1 committed; final 1000/1000/2000 OK | ✓ | A saga "abort" is not an undo: it's two real committed transactions netting to zero. 2PC abort leaves no history; saga abort leaves both — so intermediate state was genuinely visible. |
| 7 | Saga anomaly window (comp-delay 15s) + watcher + contender inside it | "(a) for 15s it print 900/1000/1900 Invariant violated, after c1 it prints 1000/1000/2000 (b) instant success, there is no lock as transaction is commited, prepared transactions hold a lock till it is commited. (c) a transaction for 1000 could decline citing low balance, while in reality alice has 1000 balance and it should suceed (d) No it can't know that it read wrong data, not marker to distinguish" | Watcher: 900/1000/1900 VIOLATED 13:48:02.995→13:48:17.585, then 2000 OK; contender SUCCESS in 2–4ms repeatedly inside the window | ✓ | Sagas trade isolation for availability: the anomaly window is real committed state — readers and writers proceed freely, decisions made on it can be wrong, and after compensation there is no marker it ever happened. |

## Prediction scorecard

**7/7 scenarios correct** (sub-part gaps: S2c — named "no built-in coordinator" but not the orphaned-prepare danger; S5c — mechanism reasoned correctly but WAL/pg_twophase unnamed).

Warm-up recall: 1/3 (missed canonical-key half of streaming-B3; missed structural mechanism in oauthjwt-B2).
